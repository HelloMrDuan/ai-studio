# V2.39.6.3.1 Installer Portability Fix Report

## 根因

V2.39.6.3 installer 将平台根目录固定为 `/root/autodl-tmp/platform-v2`，并将平台 Python 固定为 `/root/miniconda3/envs/ai-studio/bin/python`。真实 AutoDL 部署分别位于 `/root/autodl-tmp/ai-studio/platform-v2` 和 `/root/autodl-tmp/envs/ai-studio-platform-v2/bin/python`，因此 installer 在任何 baseline guard 或写入动作之前即因路径不存在而退出。

## 修改文件

- 新增 `deliverables/install_ai_studio_v2_39_6_3_stage04_full_pipeline_preflight_v2.py`
- 新增本报告

原 installer `deliverables/install_ai_studio_v2_39_6_3_stage04_full_pipeline_preflight.py` 保持不变，SHA256 仍为 `a09d9409033c37a1213298753cf43d1e188d94453c74366b2492ba02a0db5ba9`。

## 修改范围

installer 标记为 `V2.39.6.3.1-stage04-installer-path-portability`。

平台根目录发现顺序为：

1. 有效的显式 `--root` 覆盖值（如提供）
2. `/root/autodl-tmp/ai-studio/platform-v2`
3. `/root/autodl-tmp/platform-v2`

候选目录必须同时包含 `app/main.py` 与 `app/stage04_v238_runtime.py`。所有候选均无效时，错误会列出全部已检查路径并以 `not found` 结束。

平台 Python 发现顺序为：

1. 有效的显式 `--python` 覆盖值（如提供）
2. `/root/autodl-tmp/envs/ai-studio-platform-v2/bin/python`
3. `/root/miniconda3/envs/ai-studio/bin/python`
4. 当前 `sys.executable`

Python 候选必须是文件，且必须成功执行 `python --version`。所有候选均无效时，错误会列出全部已检查路径。

## 未修改业务代码声明

本补丁没有修改 `app/main.py`、`app/stage04_v238_runtime.py` 或任何其他业务源码；没有修改 Stage04、Shot、Lineage、Performance、runtime logic、verifier business logic、contract 或测试。

## Target payload SHA 保持声明

新旧 installer 的完整 `FILES` 映射逐值相等，内嵌 target payload、target SHA 和 baseline SHA 均未改变。解压后的 target payload 与当前本地 V2.39.6.3 目标源码逐字节一致：

- `app/core/task_store.py`: `508954c7e40513fe76b9e057b70d6b295c604f1ef294faabb7fc13760690c876`
- `app/main.py`: `82c5ce06876ea3f17dba1853af2b4ffcbe2c2ca13f93ea78a7005d107a12c787`
- `app/services/production_assets.py`: `7ec069ee77aa60995a9dbf929c7657bd61844e242c67050139f202079b4c819f`
- `app/services/story_continuity.py`: `cfd5d89140f10b5ab57c0745dd82fb7465159fc5e76648396c644b35a0b0eaeb`
- `app/stage04_v238_runtime.py`: `e668321b8eccf9f8adaf02452ffd5c9a0c1f0b890db4ca53ff28bd718fbdf332`

## Rollback 保持声明

rollback 机制未修改。installer 仍在写入前精确备份 live bytes 和文件 mode；失败时从该 exact live backup 恢复，并逐文件验证恢复后的 SHA256。没有加入或替换任何 baseline rollback payload。

以下安全机制均保持：baseline guard、active-task guard、exact backup、atomic write、AST/py_compile、restart、health check、OpenAPI version check、target readback、target hash readback、failure rollback、rollback hash verification。

## Self-test 结果

- Python AST / `py_compile`: PASS
- installer payload/static markers: PASS
- 真实 AutoDL 根目录候选优先级: PASS
- 旧根目录不存在时的发现逻辑: PASS
- 首选 AutoDL Python 候选: PASS
- 缺失候选回退到可执行 `sys.executable`: PASS
- `--root` 手工覆盖优先级: PASS
- exact-live-backup rollback simulation: PASS
- target payload mapping unchanged: PASS
- target payload 与本地目标源码逐字节一致: PASS

本地没有执行安装、服务重启或真实 Stage04 E2E；真实 AutoDL E2E 仍应由服务器环境单独执行。
