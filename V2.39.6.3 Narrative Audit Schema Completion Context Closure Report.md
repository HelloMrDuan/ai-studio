# V2.39.6.3 Narrative Audit Schema Completion Context Closure

## Baseline authority

The sole baseline is `v23963-current-runtime.tar.gz` (SHA256 `b333b86467682dd766f57c5ea5fb04f271e1710f9314192e4a4cb3cb00483786`). The archive was extracted independently and all three runtime files matched the supplied AutoDL hashes.

## Scope

Only `app/main.py` is written. The patch changes the active `studio_stage04_narrative_beat_audit_schema_completion_qwen32b` transport to a five-key payload containing `scene_id`, `audit_id`, `missing_fields`, `previous_audit_result`, and `required_schema`.

It excludes source text, full anchors, full Beats, narrative evidence text, duplicate spans, and retry-appended prose. Evidence IDs, Beat binding, temporal offsets, required schema, and existing audit conclusions remain available. The active llama.cpp tokenizer is required and the request is rejected before dispatch if it exceeds 6000 tokens.

No classification, MembershipRepair, Qwen request compatibility, Stage04 runtime, strict-shot-v2, production asset, temporal audit, or evidence-binding implementation is replaced.

## Transaction and rollback

The installer guards all three archive hashes, backs up their exact bytes, writes only `app/main.py` atomically, runs AST and `py_compile`, restarts the platform, verifies health/OpenAPI/readback, and restores the exact archived `app/main.py` bytes on failure. The two guarded but unwritten files are also rechecked after rollback.

## Self-test

- Archive baseline manifest: PASS
- Baseline + forward patch target SHA: PASS (`0e4fa0e14fdd3015e83fae1f55c54b12caee60d4240f1f71f4d2279ebc1742f9`)
- AST / `py_compile`: PASS
- 8000-token-level source/evidence fixture: PASS; completion payload measured 433 test tokens, limit 6000
- Main-only write scope: PASS
- Target readback: PASS
- Reverse patch and rollback archive-byte restoration: PASS
- Real AutoDL Stage04 E2E: not executed
