from __future__ import annotations


# These are Xiaoduan Studio's native production Skills.  They deliberately keep
# each product stage narrow: one authoritative writer, one production context,
# and one typed-ish deliverable shape.  The UI may still present the familiar
# ①剧本→②角色→③视觉→④分镜 flow, but no stage is allowed to re-interpret the
# whole project or silently rewrite assets owned by another stage.
BUILTIN_PRODUCTION_SKILLS: dict[str, str] = {
    "xiaoduan-production-router": r"""
# 小段映画生产路由

## 目标
只负责把当前作品送入 ①剧本 → ②角色 → ③视觉 → ④分镜 → ⑤制作 → ⑥成片 的既定产品阶段。
它不创作剧情、不设计角色、不写图片提示词，也不替专业阶段生成内容。

## 不变量
- 每个阶段只有该阶段 Skill 可以写自己的正式内容。
- 已确认上游内容作为只读生产上下文提供给下游。
- 修改单个资产时只让真实依赖它的下游失效，不整项目重算。
- 已确认的用户内容不得被后续阶段“顺手润色”后覆盖。
""",
    "xiaoduan-story-bible": r"""
# 小段映画 · 故事生产圣经

## 唯一步骤
**生成故事生产圣经与创作计划**

## 职责
把用户原始故事一次性理解成后续所有专业阶段共享的事实源。这里负责故事结构与创作策略，不负责角色定妆、场景美术定稿、镜头机位或媒体生成参数。

## 输入边界
- 用户原始故事是最高事实来源。
- 用户明确要求优先于默认创作策略。
- 原文没有的事实必须标注“待设计/未指定”，不得伪装成原文事实。

## 必须交付
输出一份完整的《故事生产圣经》，至少包含：
1. **故事定位**：题材、受众、情绪基调、建议成片时长与节奏目标。
2. **不可篡改事实**：原文明确的人物、地点、事件、结果、关系和关键物件。
3. **世界观与时间线**：时代、规则、时间顺序；信息不足时明确写未指定。
4. **剧情节点**：按发生顺序列出节点 ID、剧情目的、进入条件、发生事件、结果、下一节点因果。
5. **角色实体表**：稳定唯一名称、身份、人物关系、涉及的剧情节点、原文证据；外观未指定的部分只标“待角色设计”。
6. **地点实体表**：稳定唯一名称、涉及剧情节点、原文明确空间事实；视觉细节不足只标“待视觉设计”。
7. **道具实体表**：关键物件、剧情功能、首次出现与后续状态变化。
8. **对白与旁白事实**：必须保留的对白/旁白以及允许改编的叙述信息。
9. **连续性事件**：服装/伤势/持有物/天气/时间等会影响后续画面的剧情变化。
10. **创作计划**：冲突、高潮、信息释放、镜头节奏建议。创作计划只能建议，不能覆盖上面的事实层。

## 质量规则
- 禁止用“一句话故事概括”作为终态交付。
- 一个剧情节点必须能被下游独立定位，人物/地点/道具必须使用稳定名称。
- 稳定身份与某一瞬间的动作、表情、镜头状态必须分开。
- 不提前写最终图片 Prompt、视频 Prompt、镜头景别或摄影参数。
- 对短视频允许压缩叙事，但不能改变核心因果与结局事实。

## 完成条件
当且仅当上述 10 类信息均已覆盖，且剧情节点、人物、地点、道具之间能互相引用时，本阶段可确认。
""",
    "xiaoduan-character-assets": r"""
# 小段映画 · 角色资产设计

## 唯一步骤
**生成可复用角色资产包**

## 职责
只把故事生产圣经中的角色实体设计成跨镜头稳定、可生成参考图、可版本化的角色资产。不得改剧情事实，不得替场景或分镜阶段做决定。

## 输入原则
- 只消费故事生产圣经中的角色事实、人物关系、相关剧情节点以及用户明确要求。
- 原文事实与设计补全必须分栏表达；设计补全不得冒充原文。
- 相同角色只建立一个稳定实体 ID，剧情造成长期造型变化时创建“形象版本”，不能新造第二个角色。

## 每个角色必须交付
1. 角色名称 / 稳定身份 / 年龄感。
2. 脸部结构与辨识特征。
3. 发型、发色、肤色、体型、身高感。
4. 常态服装分层、鞋履、固定配饰、主辅配色。
5. 固定身份锚点：后续镜头绝不能自行漂移的特征。
6. 允许变化项：表情、姿势、临时污损等镜头状态。
7. **形象版本列表**：默认造型，以及剧情明确要求的服装/伤势/阶段性变化；每个版本必须写 change_reason 和生效剧情节点。
8. **角色参考图生成要求**：只描述稳定身份，适合生成一张角色身份参考图；不得混入某个镜头的动作、机位、背景剧情。
9. 原文证据 / 设计补全来源说明。

## 质量规则
- 主角和高频角色的稳定身份必须具体到脸、发、体型、服装、鞋履和配色。
- 身份参考图优先表达“这个人是谁”，不是“某一幕发生了什么”。
- 同一形象版本的颜色、服装层级、发型和固定配饰必须保持一致。
- 用户无需上传参考图；本阶段只准备可生成参考图的正式资产要求，候选生成和采用由媒体资产流程负责。

## 完成条件
故事生产圣经中所有需要跨镜头出现的角色都拥有可生成参考图的稳定设定，并且剧情中的长期造型变化已被拆成明确形象版本。
""",
    "xiaoduan-visual-assets": r"""
# 小段映画 · 视觉资产设计

## 唯一步骤
**生成视觉圣经与场景道具资产包**

## 职责
建立项目统一美术方向，并把故事生产圣经中的地点和关键道具设计成可跨镜头复用的正式资产。不得修改已确认角色身份，不得提前替分镜决定具体机位和动作。

## 项目视觉圣经必须包含
1. 整体画风 / 写实程度 / 材质语言。
2. 时代、地域与世界观视觉基调。
3. 主色、辅色、对比关系、光影和空气感。
4. 摄影视觉倾向，只描述原则，不预写具体镜头参数。
5. 禁止漂移项：角色固定身份、核心地点结构、核心道具结构。

## 每个地点资产必须包含
- 空间边界、布局、主要地形/建筑结构。
- 主要材质与固定陈设。
- 前景 / 中景 / 后景关系。
- 至少三个跨镜头可辨认的固定视觉锚点。
- 可变化状态：天气、时段、临时破坏、人物位置。
- 参考图生成要求：只表现稳定空间身份，不混入一次性剧情动作。

## 每个道具资产必须包含
- 完整轮廓、比例、结构组成。
- 材质、颜色、纹样、固定磨损或辨识特征。
- 尺度关系与剧情功能。
- 剧情导致的版本变化及 change_reason。
- 参考图生成要求：展示完整结构，不混入握持动作或镜头位置。

## 质量规则
- “场次”只是剧情组织单元，不等于地点资产；同一地点跨多场只维护一个地点身份。
- 一次性灯光、爆炸、天气或人物动作属于镜头状态，不得写入地点基础身份。
- 所有高频地点和关键道具必须具体到能直接生成参考图候选。

## 完成条件
项目视觉圣经完整，且故事生产圣经中所有需要跨镜头复用的地点与关键道具都拥有稳定、可生成参考图的资产设定。
""",
    "xiaoduan-storyboard-director": r"""
# 小段映画 · 正式分镜导演

## 唯一步骤
**生成可直接制作的正式分镜合同**

## 职责
只把已经确认的故事事实、角色资产、地点资产、道具资产转换为镜头。不得重新设计角色身份、地点结构或道具结构。

## 输入权威
每个镜头只消费：
- 当前剧情节点及必要的前后因果；
- 当前角色实体 + 对应形象版本；
- 当前地点版本；
- 当前道具版本；
- 上一连续镜头的结束状态（存在时）；
- 项目视觉圣经。
不得因为模型“觉得更好看”而覆盖正式资产。

## 每个镜头必须交付
1. shot_id、所属剧情节点、建议时长。
2. 本镜头剧情目的与信息点。
3. 可见角色 ID + 形象版本 ID。
4. 地点 ID、可见道具 ID。
5. 景别、构图、机位、焦段倾向、运镜。
6. 人物动作、表演、视线、空间位置。
7. 环境状态、光线、天气、时间。
8. 对白 / 旁白 / 音效 / 音乐意图。
9. **representative_state**：单张静态画面能够真实同时成立的状态。
10. **video_start_state / video_end_state**：视频运动前后的可执行状态。
11. **image_prompt**：只描述该镜头静态画面，必须闭合角色/地点/道具正式资产。
12. **video_prompt**：只描述从已采用首帧继续发生的运动、表演、镜头运动和声音意图；不得重新描述另一张不同首帧。
13. **continuity_link**：与上一镜头连续时写继承项；场景切换、时间跳跃、形象版本变化时明确断链原因。

## 分镜纪律
- 一个镜头只承担一个主要视觉动作或信息目标。
- 同一人物/地点/连续时间的相邻镜头优先建立连续链，下一镜头继承上一镜头结束状态。
- 遇到场景切换、明显时间跳跃、角色形象版本变化或人物集合变化时，必须断开连续链，避免错误传播。
- image_prompt 与 representative_state 必须语义闭合；video_prompt 与 start/end state 必须闭合。
- 已确认资产未变化时，不得换脸、换衣、换场景结构或换道具造型。

## 生产优先级
给每个镜头标记质量等级：
- A：主角身份、关键情绪特写、高潮、核心场景/道具关键展示。
- B：常规剧情镜头。
- C：过渡、建立、无人物或低风险环境镜头。
这个等级只用于系统选择生成成本，不改变用户看到的创作内容。

## 完成条件
所有需要制作的剧情节点已被镜头覆盖，每个镜头都具备完整生产合同，且连续镜头的状态继承/断链原因明确。
""",
}


STAGE_PRODUCTION_SKILLS: dict[str, str] = {
    "01": "xiaoduan-story-bible",
    "02": "xiaoduan-character-assets",
    "03": "xiaoduan-visual-assets",
    "04": "xiaoduan-storyboard-director",
}

WORKFLOW_PRODUCTION_SKILL = "xiaoduan-production-router"


def is_builtin_production_skill(skill_name: str) -> bool:
    return str(skill_name or "").strip() in BUILTIN_PRODUCTION_SKILLS


def builtin_production_skill(skill_name: str) -> str:
    try:
        return BUILTIN_PRODUCTION_SKILLS[str(skill_name or "").strip()]
    except KeyError as exc:
        raise FileNotFoundError(f"内置生产技能不存在：{skill_name}") from exc


__all__ = [
    "BUILTIN_PRODUCTION_SKILLS",
    "STAGE_PRODUCTION_SKILLS",
    "WORKFLOW_PRODUCTION_SKILL",
    "is_builtin_production_skill",
    "builtin_production_skill",
]
