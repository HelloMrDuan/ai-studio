# V2.39.6.3 Stage04 Performance Optimization Test Report

## Delivery scope

- Installer: `deliverables/install_ai_studio_v2_39_6_3_stage04_performance_optimization.py`
- Focused tests: `tests/test_v23963_stage04_performance_optimization_installer.py`
- The workspace production files `app/main.py`, `app/services/gemma.py`, `app/stage04_v238_runtime.py` and `scripts/start_llm.sh` were not modified.
- No real Stage04, image, video or composition E2E was executed locally.

## Baseline and target payload verification

| File | Baseline SHA256 | Installer target SHA256 |
|---|---|---|
| `app/main.py` | `82c5ce06876ea3f17dba1853af2b4ffcbe2c2ca13f93ea78a7005d107a12c787` | `91685842a3178ec8c0b3c1a36eba6c87a6cc8d7946e69771d32063350e9c595e` |
| `app/services/gemma.py` | `e50246b026bc65f4eb2b997af30004f8f9cd9f38109a06b78299a83c1ed5a4de` | `f84fe348213f88d82da87207cb473c05ce6133bdc5e30bbb21d2a98a2d9088d4` |

The installer refuses to build either target unless the live baseline bytes match these exact baseline hashes. Target hashes are checked immediately after deterministic construction and again after live readback.

## Focused performance-contract tests

Command:

```text
python -m unittest tests.test_v23963_stage04_performance_optimization_installer -v
```

Result: **9/9 PASS**.

Covered behavior:

1. Workspace business sources remain byte-identical to the guarded baseline.
2. Anchor classification contains one primary request, retains partial valid results and sends only `unresolved` IDs to repair.
3. Truncated classification JSON recovers only explicitly emitted requested IDs and leaves unseen IDs unresolved.
4. Anchor classification output budget is 750 tokens while its JSON schema remains unchanged.
5. MembershipRepair uses line mode directly, removes JSON/strict rounds and enforces a shared five-call budget per Scene.
6. Audit payload below the 6,500-token threshold is byte-for-byte the original full representation.
7. Over-budget audit payload removes duplicated narrative/evidence text while retaining source order, offsets, state, evidence IDs, spans and classification binding.
8. llama.cpp context-overflow recognition raises before the existing sleep/retry path.
9. An 8,000-token-scale source/evidence fixture produces a schema-completion payload below 6,000 tokens, with exactly five top-level fields and no source text, full anchors, full Beats or duplicated spans.

The token-guard behavior test recorded:

```text
tokens=6400 compacted=false
full_tokens=7001 compact_tokens=3200 compacted=true
schema_completion_tokens=454 limit=6000
```

## Existing protection regression

Command:

```text
python -m unittest discover -s tests -p "test_v239*.py"
```

Result: **85/85 PASS**.

This includes the V2.39.6 Qwen runtime contract, V2.39.6.1 Shot State Closure, V2.39.6.2 Narrative Lineage Closure, V2.39.6.3 full-pipeline preflight, Qwen request compatibility and the new performance installer tests.

## Installer self-test

Result: **PASS**.

Verified:

- exact baseline guard;
- deterministic target hashes;
- target AST and `py_compile`;
- one-primary partial-classification contract;
- line-only five-call MembershipRepair contract;
- real-token audit guard and compaction markers;
- context-overflow no-identical-retry contract;
- atomic target write/readback simulation;
- exact backup-byte rollback and rollback hash verification.

## Runtime acceptance still required on AutoDL

The local suite proves payload construction and deterministic contracts; it does not claim the performance target has been achieved. The next real AutoDL Stage04 run must verify:

- `context overflow = 0`;
- `repair calls <= 5` for MembershipRepair;
- execution reaches `shot_generation`;
- Scene total time and phase timing from `STAGE04_PERF`;
- Qwen output validity under the unchanged semantic/audit contracts.
